kafka.broker.list=localhost:9092
kafka.namespace=itTest

kafka.authorization.enabled=true
kafka.authorization.username=hermes
kafka.authorization.password=alice-secret

kafka.simple.consumer.buffer.size=1024
kafka.consumer.auto.offset.reset=smallest
kafka.consumer.timeout.ms=1000

admin.reaper.interval.ms=3000
oauth.server.url=http://localhost:19999

tracker.enabled=true
