zookeeper:
  enabled: false

kafka:
  clusters:
    -
      clusterName: primary
      connectionString: localhost:2181
      connectionTimeout: 3000

storage:
  connectionString: localhost:2181
  connectionTimeout: 3000

spring:
  jersey:
    type: servlet

server:
  port: 8090
  servlet-path: /status

management:
  context-path: /

schema.repository.type: schema_repo
