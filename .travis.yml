sudo: false
dist: trusty

language: java

env:
  - TEST_SUITE=integrationTest

install: ./gradlew assemble
script:
  - "cat /proc/cpuinfo"
  - "free"
  - "cat /proc/meminfo"
  - "df -h"

jdk:
  - oraclejdk8

addons:
  apt:
    packages:
      - oracle-java8-installer

branches:
  only:
      - master
      - develop

